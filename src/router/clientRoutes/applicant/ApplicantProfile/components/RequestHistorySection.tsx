import * as Tabs from "@radix-ui/react-tabs";

const RequestHistorySection = (props: any) => {
  return (
    <Tabs.Content value="request-history">
      <h2>Request History Section</h2>
    </Tabs.Content>
  );
};

export default RequestHistorySection;
