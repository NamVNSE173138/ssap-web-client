import * as Tabs from "@radix-ui/react-tabs";

const ApplicationHistorySection = (props: any) => {
  return (
    <Tabs.Content value="application-history">
      <h2>Application History Section</h2>
    </Tabs.Content>
  );
};

export default ApplicationHistorySection;
